name: Build and publish Docker image

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths-ignore:
      - 'argocd/**'
      - 'helm/**'
      - '**/*.md'
  release:
    types: [published]

permissions:
  contents: read
  packages: write
  security-events: write
  actions: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-publish-image:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write        # only this job may push the dev tag bump to main
      packages: write
      security-events: write
      actions: read
    outputs:
      short_sha: ${{ steps.sha.outputs.short_sha }}
    steps:
      - uses: actions/checkout@v4

      - name: Get short SHA
        id: sha
        run: echo "short_sha=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Build and push myapp docker image (${{ steps.sha.outputs.short_sha }})
        uses: ./.github/actions/docker-publish
        id: publish
        with:
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
          image: ghcr.io/${{ github.repository_owner }}/myapp
          dockerfile: Dockerfile
          context: .

      - name: Scan image with Trivy
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ghcr.io/${{ github.repository_owner }}/myapp:${{ steps.sha.outputs.short_sha }}
          format: sarif
          output: trivy-results.sarif
          severity: CRITICAL,HIGH
          exit-code: '1'
          ignore-unfixed: true

      - name: Upload Trivy results to GitHub Security tab
        if: always()
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: trivy-results.sarif

      - name: Bump dev image tag
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          NEW_TAG: ${{ steps.sha.outputs.short_sha }}
        run: |
          set -euo pipefail
          sed -i "s/^  tag: .*/  tag: ${NEW_TAG}/" helm/myapp/values-dev.yaml

          if git diff --quiet helm/myapp/values-dev.yaml; then
            echo "values-dev.yaml already at ${NEW_TAG}, nothing to commit."
            exit 0
          fi

          # Scope guard: refuse to push if anything other than the dev values
          # file changed, or if the diff is not a single clean tag-bump line.
          CHANGED=$(git diff --name-only)
          if [ "$CHANGED" != "helm/myapp/values-dev.yaml" ]; then
            echo "Unexpected changes, refusing to push:"; echo "$CHANGED"; exit 1
          fi
          if [ "$(git diff -U0 helm/myapp/values-dev.yaml | grep -cE '^\+  tag: ')" != "1" ]; then
            echo "Diff is not a clean tag bump, refusing to push."; exit 1
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add helm/myapp/values-dev.yaml
          git commit -m "chore(dev): bump myapp image to ${NEW_TAG} [skip ci]"
          git pull --rebase origin main
          git push origin HEAD:main
